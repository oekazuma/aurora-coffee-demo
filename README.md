# Aurora Coffee

Demo SvelteKit app used in the [svelte-vitals](https://github.com/oekazuma/svelte-vitals) promo video.

- `svelte-vitals` CLI + `@svelte-vitals/vite` dev dashboard
- `@svelte-vitals/action` runs on every pull request
- `@svelte-vitals/mcp` wired up via `.mcp.json`
